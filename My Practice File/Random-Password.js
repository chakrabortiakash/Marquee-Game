
// const passwordBox = document.getElementById("password");
// const lenght = 12;

// const upperCase = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
// const lowerCase = "abcdefghijklmnopqrstuvwxyz"
// const number    = "0123456789"
// const symbol    = "@$%#&*()_+~|{}[]></-=";

// const allChars = upperCase + lowerCase + number + symbol;

// function createPassword (){
//     let password = "";
//     password += upperCase[math.flood(Math.random() * upperCase.length)];
//     password += lowerCase[math.flood(Math.random() * loweerCase.length)];
//     password += number [math.flood(Math.random() * number.length)];
//     password += symbol [math.flood(Math.random() * symbol.length)];

//     while(lenght > password.length){
//         password += allChars [math.floor(Math.random() * allChars.length)];
//     }
//     passwordBox.value = password;
// }
